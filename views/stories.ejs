<!DOCTYPE html>
<html>
  <head>
    <title>Stories</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>Stories</h1>
    <ul>

      <% for(let i=0; i < stories.length; i++) { %>

        <li><a href="/story/<%= stories[i].slug %>"><%= stories[i].name %></a><br><%= stories[i].summary %></li>

      <% } %>

    </ul>
  </body>
</html>
